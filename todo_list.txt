1) funzione per trasformare un percorso da relativo ad assoluto. (da fare appena viene chiamato il programma, così si sa la cartella corrente.)

In Parser:
  -bool is_valid_filename (char * fn)
  -int is_valid_command(char * cmd)

In program.c
  -migliorare stampa in esegui programma con modalità verbose.

Documentazione:
  Cosa fare se specifico sia sep eche type??!?!?

2) BUGS:
  -non prende comandi con opzioni!
  - se non esiste il loggile, crearlo prima di chiamare realpath.