Per compilare:
make

Per pulire la cartella:
make clean

Per eseguire:
./logger

Bug:
Se si manda un quit troppo velocemente dopo l'avvio di logger (qualche secondo) non logga e non si chiude il demone

Per verificare se il programma è in esecuzione:
./alive logger

Per chiudere gli eventuali logger in esecuzione:
pkill logger
